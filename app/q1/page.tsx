export default function q1() {
    const arr = [1, 2, 3, 4, 5];
    const result = arr.map((item, index) => item * index);
    
    return (
        <div>
            <h1>Q1</h1>
        </div>
    )
    
}
